import { ComponentFixture, TestBed } from '@angular/core/testing';
import { CorrigelecturaPage } from './corrigelectura.page';

describe('CorrigelecturaPage', () => {
  let component: CorrigelecturaPage;
  let fixture: ComponentFixture<CorrigelecturaPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(CorrigelecturaPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
